interface:
  display_name: "AI Website Design Workflow"
  short_description: "Move from rough website inputs to design handoff and web build"
  default_prompt: "Use $ai-website-design-workflow to triage rough website inputs, create a brief, define a design thesis, sample AI outputs, validate paths, prepare Figma design handoff, implement from final design, and QA the webpage."

policy:
  allow_implicit_invocation: true
